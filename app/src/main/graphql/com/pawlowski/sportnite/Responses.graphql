query Responses($otherFilters: [OfferFilterInput!], $first: Int) {
  myOffers(where: {
    and: $otherFilters
  }, first: $first,
  order: {
          dateTime: ASC
        }
  ) {
    nodes {
      responses {
        status,
        responseId,
        offerId,
        user {
          userId,
          name,
          firebaseUserId
        },
        description,
        offer {
          description,
          dateTime,
          sport,
          isAvailable,
          city,
          street
        }
      }
    }
  }
}