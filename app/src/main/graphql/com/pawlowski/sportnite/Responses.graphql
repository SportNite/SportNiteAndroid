query Responses($sportFilter: SportType!) {
  myOffers(where: {
    sport: {
      eq: $sportFilter
    }
  }) {
    nodes {
      responses {
        status,
        responseId,
        offerId,
        user {
          userId,
          name,
          firebaseUserId
        },
        description,
        offer {
          description,
          dateTime,
          sport,
          isAvailable,
          city,
          street
        }
      }
    }
  }
}