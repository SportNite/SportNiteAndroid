query IncomingOffers {
  incomingOffers {
    offerId,
    user {
      name,
      userId,
      firebaseUserId
    }
    description,
    dateTime,
    sport,
    responses {
      status,
      user {
        name,
        userId,
        firebaseUserId
      }
    }
    city
    street
  }
}