query Users($filter: UserFilterInput) {
  users(where: $filter) {
    nodes {
      userId,
      firebaseUserId,
      avatar,
      name,
      birthDate,
      sex,
      city,
      skills {
        nrtp,
        sport,
        level,
        years,
        weight,
        height
      }
    }
  }
}