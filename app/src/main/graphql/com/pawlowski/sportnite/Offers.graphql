query Offers($offerFilterInput: [OfferFilterInput!]) {
  offers(where: {
    isAvailable: {
      eq: true
    },
    and: $offerFilterInput
  }) {
    nodes {
      offerId,
      userId,
      description,
      dateTime,
      sport,
      isAvailable,
      city,
      street,
      user {
        name,
        userId,
        firebaseUserId
      }
    }
  }
}